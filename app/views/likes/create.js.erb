$('#like-icon-post-<%= @post.id.to_s %>').
  html('<%= link_to "love", like_path(@like), method: :delete, remote: true, class: "core-sprite-en loved hide-text" %>');
$('#like-text-post-<%= @post.id.to_s %>').
  html('<%= j render "posts/like_text", {likes: @post.likes, user: @user} %>');
console.log("create like success");
